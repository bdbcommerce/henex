Build frontend assets for production using Vite.

```bash
npm run build
```

Show the output. After building, confirm:
- Tailwind CSS is compiled with brand colors (brand red #E63329)
- No Alpine.js was installed separately (it ships with Livewire 3)
- The /build/ directory was created in public/
