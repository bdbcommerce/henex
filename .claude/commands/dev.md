Start the Vite development server for hot module replacement.

```bash
npm run dev
```

Run in background. The dev server typically starts at http://localhost:5173 and Laravel's app runs via the OSPanel local server at http://henex.local (or whatever the configured local domain is).

Note: Alpine.js is bundled with Livewire 3 — do NOT install it separately.
